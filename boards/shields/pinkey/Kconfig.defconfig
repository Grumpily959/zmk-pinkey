if SHIELD_PINKEY_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "PinKey"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_PINKEY_LEFT || SHIELD_PINKEY_RIGHT

config ZMK_SPLIT
    default y

endif
